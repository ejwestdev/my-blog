[application]
# Skip the wasm-opt pass; it crashes on this toolchain (binaryen DWARF bug) and
# only affects final WASM size.
[web.wasm-opt]
level = 0

[web.app]

title = "Edward West"

[web.resource]

style = []

script = []

[web.resource.dev]

script = []
